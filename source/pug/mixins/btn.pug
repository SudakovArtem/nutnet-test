mixin btn(settings)
  if (settings.type === "link")
    a(class= (settings.cssClass) ? 'btn ' + settings.cssClass : 'btn', href= settings.href)
      span= settings.content
  if (settings.type === "button")
    if (settings.disabled)
      button(class= (settings.cssClass) ? 'btn ' + settings.cssClass : 'btn', type=settings.btnType, disabled)
        span= settings.content
    else
      button(class= (settings.cssClass) ? 'btn ' + settings.cssClass : 'btn', type=settings.btnType)
        span= settings.content
